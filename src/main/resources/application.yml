spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/spring_security_db?createDatabaseIfNotExist=true
    username: ${DB_USER}
    password: ${DB_PASS}

  flyway:
    baseline-on-migrate: true

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRE}